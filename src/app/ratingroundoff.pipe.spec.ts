import { RatingRoundoffPipe } from "./ratingroundoff.pipe";

describe('RatingRoundoffPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingRoundoffPipe();
    expect(pipe).toBeTruthy();
  });
});
