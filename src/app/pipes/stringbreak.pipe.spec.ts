import { StringbreakPipe } from './stringbreak.pipe';

describe('StringbreakPipe', () => {
  it('create an instance', () => {
    const pipe = new StringbreakPipe();
    expect(pipe).toBeTruthy();
  });
});
